# /review-tasks-plan FT-007

- Режим: bounded rerun текущей FT-007 task-planning surface.
- База rerun: предыдущий FT-007 report был `APPROVE` на Planning Revision `2`.
- Проверяемый exact delta: operator KISS decisions applied by
  `/feature-doctor FT-007`; Statistics Projection contract extended; FT-007,
  IMPL-FT-007, decision log and protocol plan updated; TASK-096/TASK-097 proof
  mappings updated. Task IDs, tier, wave, dependencies, lifecycle and index
  are unchanged.
- Authoritative decisions: one Student row per `student/class` relation;
  distinct Student-account `studentCount` for a Teacher; Teacher-only registry
  for a Teacher viewer.
- Required rerun coverage: design readiness; AC/REQ closure and ownership;
  TASK-096/TASK-097 proof scope and execution readiness; retained evidence only
  where the target, scope and governing inputs are unchanged.
- Refreshed co-review focuses: (A) acceptance closure, exact claims, ownership
  and slicing; (B) design/execution readiness, boundary ownership and T3 proof
  scope. Codex Luna/xhigh was attempted and retried once per focus; the local
  account rejected the model, so both focuses used the skill-permitted local
  read-only equivalent fallback.
- Required markers: `REVIEWED_PLANNING_REVISION: 2` and
  `ARCHITECTURE_REVIEW: not_required`.
- Review writes are limited to this REQUEST and the FT-007 final report. No
  reviewed feature, contract, plan, protocol, task card, index, lifecycle,
  scheduler or evidence artifact is edited.
