# Papercuts

- `codex exec` rejected the short `-a` option even though the top-level `codex` command documents it; the Judge launch was retried using the supported `exec` options.
- `codex exec` rejected `gpt-5.6` for the current ChatGPT account; the scheduler child was rerouted to the supported `gpt-5.6-sol` model.
