---
description: Minor workflow friction recorded during TASK-095 semantic verification.
status: active
---
# Papercut

- A read-only `git ls-files --error-unmatch` check could not validate prior
  task evidence because the current shared worktree keeps those artifacts
  untracked; the files existed and were readable, so verification continued
  using filesystem presence and content inspection.
