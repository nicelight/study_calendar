# Papercuts

- A combined read of W13 sync surfaces exceeded the tool output limit, requiring smaller targeted reads.
- A second broad validation read also truncated, so the final link and lifecycle checks were split into focused commands.
- A broad W14 boundary inspection truncated before all surfaces were visible; focused follow-up reads were required.
