## Workflow friction

- A combined `apply_patch` for FT-007 contract/plan edits failed because one
  expected protocol-plan context block differed from the current file. The
  patch made no partial changes; the edits were split into smaller patches and
  applied successfully.
- The local `codex exec` rejected the documented `--ask-for-approval` flag;
  the co-review launch was retried with the supported CLI options.
- The required `Codex Luna` co-review launches were rejected twice per focus
  because that model is unavailable for the current ChatGPT account; the
  review continued with the skill-permitted local equivalent fallback.
- A nested `codex exec ... -` launch received no prompt because the command
  wrapper did not forward stdin. It exited before starting a child or changing
  project state; retrying with the same prompt as a quoted argument worked.
- A read-only inspection assumed task cards lived inside their evidence
  directories and attempted `.tasks/<TASK_ID>/task.json`; indexed cards
  actually live at `.memory-bank/tasks/<TASK_ID>.task.json`, so the inspection
  was rerun against the authoritative index path.
