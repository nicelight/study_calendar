# Session papercuts

- `collaboration.wait_agent` rejected a 1000 ms poll because its minimum timeout is 10000 ms.
- Two bounded workers expanded reads for several minutes despite compact prompts and required interruption/replacement; target tasks were not mutated and retry budgets were unaffected.
