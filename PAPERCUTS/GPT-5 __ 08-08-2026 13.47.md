# Papercuts

- The first local task-card validation one-liner treated Markdown anchors as
  part of filesystem paths and attempted to parse YAML frontmatter as JSON;
  the diagnostic check failed before validating the edited card and was
  replaced with anchor-aware JSON/YAML-safe checks.
