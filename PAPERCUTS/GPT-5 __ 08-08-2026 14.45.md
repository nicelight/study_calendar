---
description: Minor workflow friction recorded during the autopilot session.
status: active
---
# Papercuts

- `codex exec` rejected the global approval option when it was placed after the `exec` subcommand; the child session did not start. Correct invocation places global options before `exec`.
