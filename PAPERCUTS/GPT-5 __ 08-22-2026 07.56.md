# Papercut

- The first Attempt 2 provider probe fixture failed with a SQLite foreign-key
  setup error because the cross-center parent fixture lacked the linked
  student's center membership. The fixture was corrected before the claim
  probe was accepted; the rerun then failed at the intended missing public
  query assertion.
