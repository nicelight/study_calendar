Failed initial read-only boundary probe command because over-escaped inline JavaScript regex produced a syntax error before any assertion ran; reran with simpler assertions.
Assumed `.memory-bank/tasks/index.json` was an array in a read-only jq preflight; the command stopped before assertions and was rerun after inspecting its object shape.
