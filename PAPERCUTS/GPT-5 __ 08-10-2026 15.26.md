---
description: Minor workflow friction observed during FT-003 feature semantic review.
status: active
---
# Papercut

- The TASK-014 protocol directory has no `run.md`; a read-only evidence sweep
  attempted to inspect it and returned `No such file or directory`. The
  available task protocol artifacts are `context.md`, `progress.md`,
  `handoff.md`, `verification.md`, and `red-verification.md`.

- The task-local Vitest config emitted an informational Vitest 4 warning that
  its imported `../../vite.config` path has no `.ts` extension. The focused
  semantic probe still executed and passed; no source or config was changed.
