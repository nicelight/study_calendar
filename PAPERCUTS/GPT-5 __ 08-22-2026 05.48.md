# Papercuts

- The repository Vitest default include is `tests/**/*.test.ts`; running a
  fresh verifier probe from `.tasks/` without an explicit config returned
  `No test files found`.
- The first run of the fresh probe had a verifier-only key-order assertion
  mismatch (`Object.keys(...).sort()` puts `centerId` before `classId`); no
  production source or historical evidence was involved.
- A read-only artifact-integrity command initially failed because of an
  unmatched shell quote in the command argument; the command was corrected
  without touching project state.
