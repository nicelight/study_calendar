# Papercuts

- A requested 1.28-second `wait_agent` call was silently clamped to its 10-second minimum; use the documented minimum timeout to avoid an unnecessary wait and confusing result.
- The first combined mandatory-context inspection exceeded the tool output cap and was truncated; read the remaining task-selected sections in smaller bounded commands.
