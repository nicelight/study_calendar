---
description: Minor workflow friction observed during TASK-020 verification.
status: active
---
# Papercuts

- The shell safety wrapper rejected `rm -f` for an exact disposable `/tmp`
  database path; the temporary SSR fixture was not removed by that command.
- The first verifier-owned transport probe reused a deliberately pre-bound
  provider subject for the rollback case, so the application correctly returned
  duplicate-identity `410` before the induced session-write failure. The
  disposable fixture was corrected to use a fresh subject.
- The first task-level sync patch did not apply because one expected long JSON
  line differed from the actual task record; a follow-up patch applied the
  intended evidence markers. A later ad hoc `rg` link-scan regex also failed;
  no project artifact was affected.
- The first TASK-021 verifier-owned probe run stopped at a syntax error in its
  new task-local test before any application assertion; the probe was corrected
  and the failed run is not treated as functional evidence.
- The disposable live-SSR fixture needed one warm-up request because the
  composition root creates the SQLite schema lazily; two initial seed attempts
  then exposed shell/SQLite quoting mistakes before the parameterized seed
  succeeded. No production state was involved.
