# Papercuts

- Direct Node ESM cannot import the repository TypeScript composition root (`ERR_UNKNOWN_FILE_EXTENSION`); use the bundled SQLite CLI for the disposable browser fixture instead.
- The command safety policy rejected deletion of the explicitly created temporary browser fixture; its processes can still be stopped safely, but the fixture directory must be cleaned by the environment owner.
