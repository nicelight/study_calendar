# Session papercuts

- Disposable SSR smoke seeding ran before the lazy composition root handled its first request, so the temporary SQLite database had no schema (`no such table: centers`). Bootstrap the server with a request before seeding.
- The safe shell policy rejected `rm -f` for the explicit temporary SQLite file during cleanup; no unsafe deletion workaround was used, and the file was left under `/tmp`.
