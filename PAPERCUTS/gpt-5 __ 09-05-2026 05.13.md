---
description: Evidence-backed papercut observed while executing TASK-106-T3-FT-005-W38.
status: active
---
# Papercut — required full test gate mutates ignored real database

- During `TASK-106-T3-FT-005-W38`, the disposable Playwright gate used only
  `tmp/ft-005-homework-grading-ui.db`; its exact database and `-wal`, `-shm`,
  and `-journal` sidecars were absent after each run.
- Immediately before the required full `npm run test` gate,
  `study-calendar.db` had SHA-256
  `6189ddfb68f6f65875b6e3af9cf38f9ecbe22496bd00785fd6ea1e8f8fbd93af` and
  size/mtime `372736 / 1788563944`. Immediately after the passing gate it had
  SHA-256
  `9a8ebf63d14b674bcff554db63ffe7e7b909471e534138b4e9fc85d45aeb522c` and
  size/mtime `372736 / 1788567199`.
- The file is ignored by `.gitignore` (`*.db`), so the mutation is invisible to
  `git status`. No task implementation path intentionally opened or wrote this
  database, and it was not restored because its prior contents are not
  reconstructible from the current worktree.
- Impact: the project-level `npm run test` gate is not proven isolated from
  real runtime data. This is a project-wide hygiene issue and the gate remains
  recorded as passed with the side effect disclosed.
- A later final rerun of the same required gate again passed (`76` files / `260`
  tests) while changing the ignored database hash from
  `4922a41facd27a00fd14781b2da7e740f9290ccb12f6ae26e4d22d1d4631a751` to
  `b6a478c2e7fb7a1bb4cc0b7d8c947e3eff876411e8922121e1cb23a241af2018`; the
  size remained `372736` bytes. No database restoration was attempted.
