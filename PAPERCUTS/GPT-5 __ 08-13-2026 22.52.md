- A combined disposable-CLI probe was rejected before execution because the
  command included `rm -f` cleanup syntax. The evidence path was split into
  the focused isolated test and a non-mutating runtime import/TTY rejection
  probe instead.
