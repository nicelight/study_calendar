# Papercuts

- `review-tasks-plan` requires two `Codex Luna` co-reviewers, but the available
  `spawn_agent` model list exposes only `gpt-5.6-sol` and `gpt-5.6-terra`.
  Both focus launches were retried once as required and rejected before start;
  review continued through the skill's explicit unavailable-model fallback.
