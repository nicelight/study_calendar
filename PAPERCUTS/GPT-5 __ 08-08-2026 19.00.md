# Session papercuts

- Vitest's project config limits discovery to `tests/**/*.test.ts`, so an explicitly targeted verifier artifact under `.tasks/` returned `No test files found`. A task-local Vitest config was required to run the verifier-owned probe without moving it into production test scope.
- The first Attempt 2 bounded verifier fixture duplicated a lesson date already generated by `createRecurringSchedule`, causing a disposable SQLite uniqueness failure before the financial path; the fixture was corrected to use generated lesson IDs.
