# Session papercuts

- A read-only route probe was rejected because the shell command bundled `rm -f`; rerun without cleanup commands.
- A Node HTTP smoke probe used CommonJS `require` with top-level `await`, which Node rejected as ambiguous; rerun inside an async function.
- A direct browser date-mask smoke was attempted before SvelteKit hydration finished; the first fill appeared inert. Waiting for the client runtime before interaction made the same smoke pass.
