# Papercut

- A verifier-owned inline disposable browser probe exceeded the command timeout and left its owned Vite child process running; it was stopped explicitly. The disposable database and `study-calendar.db` metadata remained clean.
