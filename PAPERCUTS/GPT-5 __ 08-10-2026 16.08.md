# Papercuts

- A sync-local `jq` assertion assumed `.memory-bank/tasks/index.json` was an
  array; the file is an object with a `.tasks` array. The assertion stopped
  before changing project data and was corrected after inspecting the shape.
