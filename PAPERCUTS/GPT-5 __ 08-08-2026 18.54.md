# Papercuts

- A handoff protocol patch initially failed because the expected context line
  did not match the current file; rereading the small protocol file and
  applying a narrower patch resolved it.
