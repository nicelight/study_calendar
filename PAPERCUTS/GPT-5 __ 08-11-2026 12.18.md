# Papercuts

- Focused task test initially referenced `root` from a sibling `describe`
  block, causing a local `ReferenceError` before the production-boundary
  assertions ran. Fixed the fixture scope and added explicit database cleanup;
  no production behavior or task boundary changed.
