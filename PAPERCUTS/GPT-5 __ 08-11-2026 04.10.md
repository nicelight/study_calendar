# Workflow papercuts

- Vitest verifier configs are task-specific and each config includes only its own probe; passing multiple task config paths to one `vitest run --config` invocation produced `No test files found` before the probes ran. Re-run each verifier config separately.
