- `jq '[.[] | select(.id==...)] | length' .memory-bank/tasks/index.json` failed because the authoritative index is an object with a `.tasks` array, not a root array. Corrected query: `jq '[.tasks[] | select(.id==...)] | length'`.
