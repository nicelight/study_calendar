Inline semantic probe setup initially failed because shell single-quoting stripped SQL string literals from the `tsx -e` command; SQLite then interpreted fixture values as column expressions. The probe was stopped before any assertion and will be rerun with safe quoting.
The first quoting correction used SQLite double-quoted values, which this SQLite build treats as identifiers; the probe was again stopped before assertions. Parameterized fixture inserts avoid both shell and SQL quoting ambiguity.
