# Papercuts

- Initial disposable-fixture seeding used SQLite double quotes for string values, so SQLite treated an ID as a column and rejected the command before inserting data; bound parameters avoid the quoting ambiguity.
- An optional inline data-URL expansion of the browser probe hit nested shell/template-literal quoting before Node started; keep verifier probe variants in a dedicated artifact or use the already-inspected stable driver instead of multi-layer inline rewriting.
