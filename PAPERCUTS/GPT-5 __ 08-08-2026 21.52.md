# Papercuts

- Initial sync-local validation used escaped `|` in an `rg` basic-regex
  pattern, which made a valid RTM row appear missing; rerun with literal
  matching confirmed the row.

- A fresh feature Reviewer launch requested unsupported `gpt-5.2-high` metadata
  under the active Codex account and exited before any protocol or verdict
  artifact was written; rerun with the default available model.
