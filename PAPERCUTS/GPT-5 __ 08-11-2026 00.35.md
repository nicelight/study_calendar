Patch application failed because the feature file context had changed from the
expected block; the durable repair was to re-read the file and apply smaller
context-specific patches.

An inline Node runtime probe could not load TypeScript because this Node build
reported `ERR_NO_TYPESCRIPT`; continue with the project-native Vitest runner.

An inline transpiled Google adapter probe failed because the nested data-URL
module import was encoded incorrectly; no project state changed.

The first evidence append used a non-schema `verification` task field; it was
immediately moved into the existing schema-approved `verify` array.
