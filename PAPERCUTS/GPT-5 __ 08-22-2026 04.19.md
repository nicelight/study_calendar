# Session papercuts

- The new ordinary-vs-disposable Playwright selection regression exceeded Vitest's default 5-second test timeout because it runs two real `npm run e2e -- --list` subprocesses; the test was given a local 15-second timeout, with no production or task-lifecycle change.
