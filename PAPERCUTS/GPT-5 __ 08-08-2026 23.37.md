# Papercuts

- The repository's project Vitest configuration is named `vite.config.ts`,
  not `vitest.config.ts`; a read-only inspection using the conventional latter
  name failed before discovery found the configured file.
