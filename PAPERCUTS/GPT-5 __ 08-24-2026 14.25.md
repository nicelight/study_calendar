# Papercuts

- Mandatory `finding-adjudication` co-reviewer launch failed twice before a thread was created (`no thread with id`), so `/verify` must use the skill's documented continue-without-co-reviewer fallback.
- One combined `apply_patch` replacement of the existing verification protocol was rejected because the patch engine does not allow delete-and-add operations for the same file in a single patch; the protocol update must use an ordinary full-file update instead.
