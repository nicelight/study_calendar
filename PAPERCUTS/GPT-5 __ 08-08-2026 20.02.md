# Papercuts

- Initial multi-file `apply_patch` was rejected because one SQL line in an added hunk lacked the patch `+` marker; the changes were reapplied successfully in smaller hunks.
- A combined final shell check had a quoting typo and exited before running the checks; the checks were rerun with separate safe arguments.
