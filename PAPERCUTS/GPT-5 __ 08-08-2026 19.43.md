- Initial sync-local Node assertion assumed fixed `verify` array positions and
  reported a false mismatch; the task record was re-read by `stage`/`attempt`/
  `verdict` and the validation was rerun successfully.
