# Session papercuts

- Vitest 4.1.10 does not accept the expected `--include` CLI option; selecting a verifier-owned `.tasks/` probe therefore requires a task-local config (or another config composition) instead of a direct include override.
- The first inline Vite runtime probe had a missing closing parenthesis; no project state or source was changed, and the probe was rerun with corrected syntax.
- `npx tsx -` executed stdin as JavaScript for this harness, so a TypeScript `as const` assertion caused a syntax error; no project state or source was changed, and the probe was rerun without TS-only syntax.
- A compact `jq` assertion iterated the mixed `verify` array as if every item were an object, so it failed on the two string criteria before validating the current semantic entry; the card was re-read with a type-aware selector.
