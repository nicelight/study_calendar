`npm exec vitest run <file> -t <name>` did not forward the test-name filter in
this environment and silently ran the complete file. Invoke the local
`node_modules/.bin/vitest` binary when a focused verifier probe is required.

The installed Node.js v22.22.1 reports `ERR_NO_TYPESCRIPT` for
`--experimental-strip-types`; this build lacks TypeScript support despite
recognizing the flag. Use the project test/build toolchain for TypeScript runtime probes.

A long `node -e` boundary assertion lost nested quotes around a `$lib` import
substring when embedded in the shell command. Prefer quote-free substrings or
a smaller assertion expression for inline read-only source probes.

The first private-root-channel assertion scanned the whole returned object and
mistook passing the issuer into `CenterSchedulingBoundary` for exporting the
issuer. Bound privacy checks to the declared public `CompositionRoot` shape.
