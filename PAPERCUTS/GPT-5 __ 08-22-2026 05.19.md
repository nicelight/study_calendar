## Workflow papercut

- A discovery command included the absent `vitest.config.*` glob, producing a harmless `No such file or directory` diagnostic. The repository uses `vite.config.ts`; subsequent inspection was routed there.
- The first verifier probe invocation used the repository test script, whose configured include is `tests/**/*.test.ts`, so the `.tasks/` probe was not discovered. The probe remains intact; execution was rerouted through an explicit Vitest config/include override.
- Vitest 4 in this repository does not expose the attempted `--include` CLI option. A disposable config-local include was used instead.
- The first independent probe expected class rows in insertion order, while the normative provider query sorts by class name; the probe was corrected to assert the observed contract order.
- The corrected probe also listed sorted object keys in a non-lexical order; this was a verifier-test assertion mistake, not a product observation, and was corrected before accepting results.
- A historical TASK-094 report path from the comparison command did not exist in this checkout; the available verification handoff was still read, and no historical artifact was modified.
