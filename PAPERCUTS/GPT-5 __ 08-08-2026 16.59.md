- A final compound shell validation joined several silent predicates with
  `&&`; one predicate returned non-zero without identifying the condition.
  Re-running the checks independently showed every task condition valid.
  Prefer separately reported checks for multi-condition handoff validation.
