# Workflow papercuts

- The installed `feature-to-tasks` skill was listed under the project skills root, while an initial lookup used the global Codex skills path and failed; the project-local path resolved the skill.
- The installed Ajv version emitted a deprecated-option warning for `jsonPointers` during schema validation; validation was rerun successfully with the loaded schema and supported options.
- A first jq index-invariant probe applied `map` at the root instead of `.tasks` and emitted `Cannot index number with string "id"`; the structural check was rerun with an explicit Node probe and passed.
