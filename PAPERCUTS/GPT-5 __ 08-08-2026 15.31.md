# Papercuts

- Инспекционный `git diff --no-index /dev/null <new-file>` вернул штатный exit 1
  из-за найденных различий, из-за чего составная read-only команда выглядела как
  failed tool call. Для просмотра untracked files запускать его отдельно с явно
  обработанным ожидаемым exit code.
