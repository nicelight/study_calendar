`vite.config.ts` limits Vitest discovery to `tests/**/*.test.ts`; invoking a
new verifier-owned probe from `.tasks/` therefore returned `No test files found`
until the probe was run from the disposable `tests/` path.

The first task-local probe transform then exposed a parenthesis error in the
new harness; no production module was involved and the probe was corrected
before evidence collection continued.
