# Session papercuts

- A fresh child `/exe` launch with local Codex `-m gpt-5.6` was rejected before execution because the ChatGPT account does not support that model. No project task state or evidence was changed; retrying with the available default model.
- The first verifier-owned profile probe run failed before executing its assertions because the probe imported `IdentityAccessBoundary` from the internal writer module; the probe was corrected to import the public boundary, with no production or task lifecycle change.
- The corrected probe then used `ORDER BY 1, 2, 3` against tables with fewer than three columns; SQLite rejected it before assertions. The disposable probe was narrowed to `ORDER BY 1` and rerun, with no production or lifecycle change.
- `vite-node --script` does not accept stdin without a script path and failed before running an extra disposable probe; no project state changed.
- A first `rg` structural search used unsupported look-around syntax and failed before reading files; the same checks were rerun with supported searches, with no project state change.
- Passing `/dev/stdin` as the `vite-node` script path also fails module resolution before execution; no project state changed.
- A broad combined context read was truncated by the tool output limit; the affected sources were reread in smaller bounded chunks, with no project state changed.
- A combined implementation patch was rejected by `apply_patch` because one added line lacked a patch prefix; the same scoped files were applied in smaller valid patches.
- The first build rejected a named helper export from `+layout.server.ts` under SvelteKit route export rules; the helper was made private and the build passed.
- The first disposable E2E attempt rejected the runner's absolute `DATABASE_URL` in the spec guard, and the next attempt used an unsupported Playwright Response matcher; both were corrected inside task scope.
- An intentional occupied-port cleanup probe waited on Playwright's configured web-server timeout; the agent-owned probe processes were stopped, the exact disposable target was absent, and a deterministic injected-failure cleanup test replaced the slow probe.
- Playwright emits non-blocking `NO_COLOR`/`FORCE_COLOR` warnings during disposable runs; gate results remain green.
- A fresh `/red-verify` child launch initially passed the parent-only `-a`
  approval option to `codex exec`, which rejected it before child creation; no
  project state or task evidence changed. The launch was retried with the
  supported `codex exec` options.
- A fresh `/red-verify` launch command had an unmatched shell quote and failed
  before child creation; no project state or task evidence changed. The
  command will be rerun with corrected quoting.
- The first fresh TASK-080 `/verify` completed all verifier-owned checks but
  its optional co-review orchestration remained idle in `ep_poll`/`futex` with
  no durable verdict for several minutes. The stale reviewer context was
  terminated after preserving its evidence scaffold; a fresh replacement
  verifier was launched without changing task lifecycle or implementation.
- The first fresh TASK-080 semantic-fail Judge consultation loaded the policy
  and evidence surface but remained in nested `collab: Wait`/`ep_poll` without
  returning the required assessment. Its context was terminated after
  preserving all red-verification evidence; a direct replacement Judge was
  launched without changing lifecycle, implementation, or checkpoint route.
- The initial fresh `/feature-to-tasks FT-007` launch was rejected by
  `codex exec` because that subcommand does not accept the top-level `-a`
  approval flag; no child was started and no project state changed.
- The corrected launch then exposed that `--no-alt-screen` is also a
  top-level-only option unavailable to `codex exec`; no child was started and
  the same launch is being retried without it.
- The first planner retry with explicit `-m gpt-5` was rejected by the
  ChatGPT account before project reads (`gpt-5` is unsupported for Codex); no
  planning or project state changed, so the fresh planner is being relaunched
  on the configured default model.
- The first fresh TASK-PLAN reviewer reached a nested `collab: Wait` without a
  durable verdict for about seven minutes. Its exact reviewer/co-reviewer
  process tree was terminated after preserving review artifacts; a replacement
  reviewer is being launched with explicit bounded no-nested-wait recovery.
- The TASK-089 `/exe` completion patch initially missed the current handoff
  wording and was rejected by `apply_patch`; no project state was lost and the
  child is retrying the durable evidence update.
- A follow-up TASK-089 `/exe` progress patch also matched stale expected lines;
  it was retried against the current artifact without loss of implementation or
  evidence.
- The first TASK-089 `/verify` completed its independent probe and full suite but
  then remained idle in `ep_poll/futex` without writing a verdict; its evidence
  was preserved and the exact reviewer session was interrupted for replacement.
