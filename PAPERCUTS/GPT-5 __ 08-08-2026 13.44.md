# Papercuts

- A compact `jq` index probe assumed every task had iterable `.depends_on`; a Foundation record contains `null`, so the probe exited non-zero before the targeted-card assertions. The targeted structural probe was rerun with explicit task-card checks and passed.
