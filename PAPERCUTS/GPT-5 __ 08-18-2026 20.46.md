# Papercuts

- Several broad parallel context reads exceeded the tool output budget and truncated the combined output; reran the required reads as smaller focused probes.
- The first `mb-lint` pass rejected a task's mixed-case Architecture Spine anchor; normalized the anchor to the repository's lowercase slug and reran the targeted validation.
- A standalone heading-anchor probe reported false positives for the repository's accepted AC-prefix and workflow-anchor conventions; treated `mb-lint` plus targeted FT-007 locator checks as authoritative.
