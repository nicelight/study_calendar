# Papercuts

- `Object.freeze` widened the private authorization brand value from literal
  `true` to `boolean`, so the first `npm run check` failed until the brand was
  explicitly preserved with `true as const`.
