# Papercuts

- The first delegated review launch used the wrong structured-input shape and
  failed before the reviewer started; the retry with the supported plain
  `message` input completed successfully.
- A large PRD patch initially failed because one nearby paragraph had already
  changed; smaller anchored patches were used to preserve unrelated wording.
- A broad legacy-scope grep flagged FT-007's explicit prohibition of the old
  Global Admin model; the validation pattern was narrowed to distinguish
  normative exclusions from active scope claims.
