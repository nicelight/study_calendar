# Papercuts

- A read-only `jq` summary assumed that `.memory-bank/tasks/index.json` embedded task status fields; its compact index contains only task IDs and filenames, so authoritative status checks must read the indexed task cards.
- A broad lifecycle search named a nonexistent `.memory-bank/workflows/autopilot.md`; the scheduler contract is supplied by the installed skill and the project `autonomy-policy.md`, so later inspection was narrowed to existing workflow files.
