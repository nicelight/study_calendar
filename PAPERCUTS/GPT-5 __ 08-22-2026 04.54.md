The first reviewer-owned inline Node probe failed before execution because the shell expanded JavaScript `${...}` expressions in the command string. The command was retried with quoting corrected; no project behavior or evidence was changed.
