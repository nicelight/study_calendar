# Papercuts

- A broad jq preflight probe incorrectly iterated each task object as an array,
  producing harmless type errors; the selected task's proof path was checked
  directly and future queue inspection uses the task index as the array source.
