---
description: Minor workflow friction observed during TASK-014-T3-FT-003-W8.
status: active
---
# Papercuts

- The first task-local Vitest config used `../../../vite.config` from the
  `.tasks/TASK-014-T3-FT-003-W8/` directory, which resolved one level too high;
  the disposable config needs `../../vite.config` to reach the project root.
  The command failed during config loading before executing the probe; the
  path was corrected before treating the result as RED evidence.
- The same disposable probe initially imported project source with
  `../../../src`; from its task directory the correct relative path is
  `../../src`. That second config/import failure also happened before the
  claim body ran and was corrected without changing production source.
