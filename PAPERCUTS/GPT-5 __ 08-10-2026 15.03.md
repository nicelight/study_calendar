# Papercuts

- The first fresh verifier probe was stored under `.tasks/`, while the
  project Vitest include only covers `tests/**/*.test.ts`; the direct run
  therefore reported `No test files found`. A task-local Vitest config with an
  explicit include/exclude surface is used for the replacement probe.
