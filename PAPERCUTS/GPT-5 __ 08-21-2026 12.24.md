# Papercuts

- Диагностическая команда `jq` для зависимостей task cards предположила, что каждый элемент `depends_on` — объект с полем `status`; в текущем формате элементы содержат строки, поэтому выборка завершилась ошибкой. Повторная проверка выполняется по прямым полям task cards.
