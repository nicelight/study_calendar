- Initial verification preflight included one non-existent `.memory-bank/memory-bank/index.md` path, producing a harmless missing-file diagnostic.
- The first isolation probe used `require()` inside an ESM stdin script and failed before running the probe; the probe was corrected to use an ESM import.
- An ad-hoc task preflight initially looked for `forbidden_scope` at the top level instead of under `runtime_context`; the check was corrected before recording evidence.
- An inline Playwright diagnostic used nested shell/JavaScript/SQL quoting and reached a Node `SyntaxError` before starting; rerun with parameterized SQL and safer quoting.
- The corrected browser diagnostic then used an ambiguous `header` locator and stopped on Playwright strict-mode before the click-state observation; rerun with the task shell's `data-protected-shell` selector.
- The successful inline browser probe left its npm/Vite child keeping the PTY open after the parent kill; an explicit interrupt stopped the probe, no server remained, and the exact disposable target was absent.
