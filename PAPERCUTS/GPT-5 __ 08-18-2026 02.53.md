# Papercuts

- A multi-file `apply_patch` reconciliation failed on exact context in the FT-005 decision log and FT-006 plan; split patches and inspected the local text before retrying.
- The system `ajv` default class rejected the task schema's draft-2020 `$schema`; rerun uses the bundled draft-2020 entrypoint.
- An ad-hoc task-index probe initially parsed the `FT-<NNN>` segment at the wrong split position and emitted false feature mismatches; corrected the probe and reran it.
- An ad-hoc task-index probe initially treated the object-shaped index as an array; corrected the probe to read its `tasks` collection.
- The first TASK-044 isolated probe had an over-narrow group-charge array
  expectation; treated as a harness error rather than RED, corrected the
  expectation, and reran the probe successfully.
- The first TASK-045 ownership scan included a non-existent
  `src/lib/server/modules/calendar` path and emitted an `rg` path error; the
  scan was rerun against the actual consumer/module paths and passed with no
  matches.
- The first strict-doctor closure check rejected the otherwise complete
  TASK-045 acceptance trace because the progress label used plural
  `accepted claim locators`; aligned it with the validator's singular
  `accepted claim locator` contract and reran the gate.
- The first TASK-046 authority probe used the wrong payment-audit table name;
  corrected the harness to `financial_payment_audit_records` after schema
  inspection. This was a test setup error, not claim-specific RED.
- The first W23 sync-local probe treated the task's lifecycle-closure object as
  a report-bearing verification object and checked an undefined path; narrowed
  the probe to `functional`/`semantic` stages before rerunning it.
- The first TASK-048 retry probe expected a second allocation even though the
  first payment already covered the sole charge; corrected the harness to
  expect one allocation and explicit excess for the distinct second payment.
- The first TASK-049 semantic source assertion rejected the existing Lesson
  Context module's legitimate `lesson_context_material` SQLite access; narrowed
  the assertion to forbidden financial tables only.
- The first TASK-050 final `git diff --check` found one trailing space in the
  new FT-006 closure note; removed it and reran the hygiene gate.
