# Papercuts

- The initial `rg --files` discovery silently omitted all dot-directories even
  with matching `-g` patterns; project workflow artifacts became visible only
  after adding `--hidden`.
- Another workspace process appended a semantic verdict and changed the task
  lifecycle between the pre-write snapshot and the evidence patch. The atomic
  patch failed instead of partially overwriting the concurrent state, so the
  later report had to use a new standalone artifact number and explicitly
  supersede the intervening conclusion.
