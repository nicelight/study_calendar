# Papercuts

- An inline sync-local Node validation expression contained Markdown backticks
  inside a double-quoted shell argument; Bash attempted command substitution
  before Node started. The validation still passed, but future checks should
  avoid backticks in shell-quoted expressions or use a literal-safe script.
