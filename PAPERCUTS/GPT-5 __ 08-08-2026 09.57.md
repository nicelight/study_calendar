# Papercuts

- Inline queue validation initially failed because an over-escaped JavaScript regular expression was malformed; rerun with a simpler path matcher.
- `npm install` initially hit `ERESOLVE` because the registry's TypeScript 7 latest conflicted with `svelte-check`'s TypeScript 5/6 peer range; pinning the compatible TypeScript 5 line is sufficient.
- Sync-local jq assertion applied `length` to a boolean because of filter precedence; the assertion was corrected without changing project data.
