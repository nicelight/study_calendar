# Papercuts

- The repository Vitest config includes only `tests/**/*.test.ts`, so the
  first task-local RED probe command against `.tasks/.../red-probe.test.ts`
  returned `No test files found` before loading the probe. Re-running through a
  task-local config is required for task evidence.
- The first task-local config omitted the repository's SvelteKit Vite plugin,
  so the alias `$lib/...` could not resolve. The config was corrected to load
  the same plugin before rerunning the probe.
- An initial `apply_patch` for the post-stall gate section missed its exact
  context; the same durable update was applied with a narrower anchor.
