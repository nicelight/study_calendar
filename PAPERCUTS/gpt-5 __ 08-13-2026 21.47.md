- Inline Node validator failed on a JavaScript parenthesis typo before running; rerunning it as a simpler script resolved the workflow interruption.
- The generic Markdown-anchor validator initially treated `requirements.md` references as heading anchors, while this project resolves `REQ-*` locators through requirement records; the validator needed a project-specific REQ-ID check.
- A queue validator assumed task-index entries contained a `path` field; this index contains identities/metadata only, so task-card paths must be derived from the canonical filename convention.
