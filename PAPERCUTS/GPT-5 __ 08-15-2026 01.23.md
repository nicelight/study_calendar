# Papercuts

- A multi-file `apply_patch` failed because the final handoff line differed
  from the planned context. Read the current target blocks immediately before
  composing one large context-sensitive patch, or split independent updates.
