# Papercuts

- `apply_patch` rejected a combined delete/add operation targeting the same Svelte file; split the replacement into separate patches.
- The disposable browser probe needed a short post-navigation hydration wait before exercising client-side sorting; without it, the click observed SSR markup before the client handler was ready.
