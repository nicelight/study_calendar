# Papercuts

- `apply_patch` failed while appending retry evidence because the selected anchor used a level-two heading while the file used a level-three heading. Inspect the exact tail/heading before a multi-file evidence patch.
