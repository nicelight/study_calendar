- Focused disposable Vitest probe under `.tasks/TASK-016-T3-FT-004-W6/` was not discovered when invoked with the root `vite.config.ts`, because that config includes only `tests/**/*.test.ts`; the runner returned `No test files found` with exit code 1. A task-local config is required for this evidence surface.
