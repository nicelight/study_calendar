- `vi.fn(async () => ({ provider: 'google', ... }))` widened the provider literal
  to `string` under the project typecheck; preserving it with `as const` is
  required for the normalized provider adapter contract.
- Several invitation tests used `2026-08-12` as a supposedly future expiry and
  began failing when the calendar reached that date; valid fixtures need a
  durable future date or an injected clock.
