# Papercuts

- A dependency-status `jq` query assumed every indexed task had `depends_on`; a null value caused the otherwise read-only diagnostic command to fail. Use `(.depends_on // [])` before iterating.
