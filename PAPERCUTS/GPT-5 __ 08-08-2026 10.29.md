---
description: Session papercuts for GPT-5 on 2026-08-08.
status: active
---
# Papercuts

- Plain `node --input-type=module` cannot import the repository's `.ts` source
  files and reports `ERR_UNKNOWN_FILE_EXTENSION`; the disposable smoke fixture
  was seeded with the installed SQLite driver instead.
- The shell safety wrapper rejected `rm -rf` even for the exact disposable
  fixture path; cleanup was performed with an exact `fs.rmSync` target.
- A first exact-context `apply_patch` for the spec-backbone router missed
  because the wrapped Markdown lines were copied as one line; rereading the
  local fragment and applying a narrower patch resolved it.
- A sync-local `jq` assertion initially applied `join` across the whole object
  because of filter precedence; the validation was rerun with explicit array
  construction and did not change project data.
