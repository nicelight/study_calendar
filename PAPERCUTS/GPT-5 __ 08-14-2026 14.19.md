# Papercuts

- Initial task-state query had the JSON filename placed before the complete `jq` filter, so Bash parsed the remaining filter as shell syntax. Re-ran with the complete filter quoted.
- Attempted a collaboration message through the `functions.exec` namespace; collaboration tools are only available as direct calls. Re-sent successfully through `collaboration.send_message`.
